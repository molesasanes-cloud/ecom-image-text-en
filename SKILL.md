---
name: ecom-image-text-en
description: 将电商详情页、主图和 SKU 图片中的中文文案改成英文，用真实字体排版并逐处检查补字痕迹。适用于要求保留原图场景、商品和尺寸的高保真图片本地化任务。
---

# 电商图片仅改字英文化

## 目标

把图片中的用户可见中文文案翻译为自然、准确的英文，并交付与源图同宽同高的图片。源图始终保留。人物、商品、背景、光影、Logo、价格和数字等非目标内容不得改动。英文必须用真实字体确定性排版。可见的色块、接缝、残字、重影、变形和细节损伤均为验收失败，不能把有瑕疵的图标为成品。

## 工具边界

- 优先在原图上清除旧字，再用 `scripts/typeset-english.ps1` 绘制准确英文。平滑渐变背景优先用四边插值补底；只有单向变化可忽略时才用水平插值，纯色圆角底板可用 `pill` 重建。复杂纹理上的旧字可用 Codex 内置 `image_gen` **只做擦字和补背景**，不要让它绘制最终英文。不要调用图像 API、CLI、第三方修图服务，也不要索取 API Key。
- 如果 Codex 当前明确提供模型选择且可选 `gpt-image-2`，选用它。若内置工具不暴露模型选择，照常使用内置编辑，但向用户说明无法核实或强制指定底层模型。绝不声称已锁定 GPT Image 2。
- 内置图片编辑可能重绘非文字区域。提示词中的“保持不变”不是像素级保证。若曾用它擦字，先检查补背景和尺寸，再用 `scripts/lock-text-regions.ps1` 将**经人工核对的文字区域**贴回原图画布；文字区域外直接保留源图像素。脚本默认在框内 12 像素范围柔和接合，故框边必须留有至少 12 像素无旧字、无商品的安全边距；不满足时须调整框或改用更精细遮罩，不能让旧字混出重影。候选比例相同但尺寸不同时可用 `-NormalizeSameRatio`。最终英文仍须由排版脚本写入。

## 整套详情图

可以一次处理一个文件夹中的详情图、主图和 SKU 图。先按文件名自然顺序列出全部图片，逐张记录尺寸与文案，建立共用的品牌词、材质词和卖点术语表。先交付一张样张确定英文风格，再逐张编辑；每张都从自己的原图出发。对同类标题、规格和说明文字共用字体、字重、字号和行距；若最长一项放不下，统一缩短文案或统一调整该层级字号，不逐张随意缩放。按原文件名加 `_en` 输出。每张独立检查尺寸、拼写、遗漏和文字框外像素，最后给出总数、通过数及需复核清单。

## 操作流程

1. **读取原图。** 记录每张图片的文件名、像素宽高、格式；逐张查看，不把一套文字猜测套用到全部 SKU。区分设计文案、Logo、商品包装文字、规格、价格、型号、认证标识。只处理用户指定或确认要翻译的部分；无法确认含义的产品参数先标记。
2. **建立文字与版式清单。** 对每张图列出原文、拟用英文、位置和保留不译项。记录每类文字的字体、字号、字重、颜色、对齐和文字框。翻译保留产品事实、数字、单位和法律/认证表述，不增加卖点。英文偏长时先精简译文，再在同一层级统一改字号；禁止非等比拉伸字母。
3. **只清除旧字。** 在排版 JSON 的 `erase` 中指定经过目视核对的区域。像示例图中同时存在横向与纵向变化的浅色渐变，使用 `interpolate-four-sides` 从四周干净像素补底；`interpolate-horizontal` 仅限上下没有可见变化的背景；`pill` 仅限可完整重建的圆角底板。擦字框应只覆盖旧字及必要留白，边界不能穿过字形、商品或纹理。若背景含复杂纹理、投影或人物商品，使用内置 `image_gen` 仅擦旧字并补背景，明确要求**不要绘制英文**。任何方法留下可见色块或接缝时改用可编辑源文件、重新取得干净底图，或标为需设计师复核。
4. **锁定干净底图。** 若曾使用生成式擦字，将经核对的擦字区域写成 JSON 数组，每项含 `x`、`y`、`width`、`height`，运行 `scripts/lock-text-regions.ps1 -Source <原图> -Candidate <擦字候选> -Regions <区域.json> -Output <干净底图.png>`；默认 12 像素柔和接合，可用 `-FeatherPx` 调整。柔和范围内必须只有背景，没有旧字或目标细节。候选比例相同但尺寸不同时可加 `-NormalizeSameRatio`。这样干净底图与原图尺寸一致，修改框外保留原图像素。若无生成式擦字，直接以原图为底图。
5. **确定性排版。** 在 JSON 的 `text` 中逐项写入准确英文与固定文字框、字体、字号、字重、颜色和对齐；运行 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/typeset-english.ps1 -Source <原图或干净底图> -Layout <排版.json> -Output <成品.png>`。脚本使用 Windows 系统字体，检查宽高溢出，按固定字号绘制，不压扁、拉长或自动缩字。溢出时修改文案/布局后重跑；使用的字体在交付环境中须实际存在。只有原文字区域及必要的底板可改变，不能覆盖人物、商品或 Logo。
6. **逐张验收。** 检查输出与源图宽高一致；在 100% 原始像素视图和 200%–400% 放大视图逐处查看新字及四周背景，同时看整图缩略图。检查字形、字距、字重、基线、换行、同层级字号、拼写、产品名称、数字、旧字残影；特别查找整块颜色变化、矩形边、光影断层、重复纹理和羽化重影。对照原图做像素差异，确认修改区域外完全不变。必要时裁出每个修改框连同四周留白供复核。任何一处可见瑕疵都重做该处；无法无痕修复时标为未完成，不能交付为成品。
7. **交付。** 原图与成品分开保存，文件名保留原名并加 `_en`。给出逐图的中英对照及未处理/需复核项。多张图先做一张样张供用户确认英文风格，再继续整批；若用户已提供完整术语和风格要求，可直接执行。

`typeset-english.ps1` 的布局文件示例（坐标单位为原图像素）：

```json
{
  "erase": [
    {"x": 503, "y": 31, "width": 278, "height": 50, "mode": "pill", "radius": 25, "colorLeft": "#CD809F", "colorRight": "#CA809F"},
    {"x": 546, "y": 94, "width": 202, "height": 34, "mode": "interpolate-four-sides"}
  ],
  "text": [
    {"value": "Vienna Pink + Mossen Green", "x": 508, "y": 36, "width": 268, "height": 38, "font": "Segoe UI", "size": 19.5, "bold": true, "color": "#FFFFFF", "align": "center"}
  ]
}
```

`erase` 可为空数组；复杂背景先生成干净底图时无需再局部擦字。`interpolate-four-sides` 要求擦字框四周紧邻像素均为干净背景；它适合平滑颜色变化，不适合纹理或物体边缘。每个 `text` 项的框必须容纳完整文案，脚本会在溢出时报错。

## 编辑提示词骨架

```text
Use case: text-localization
Edit target: the supplied original ecommerce image.
Task: erase only these Chinese text items and reconstruct the background beneath them:
1. [位置] “原文”
2. ...
Do not write, draw, or invent any English text. Preserve the exact scene, people, faces, poses, product geometry, colors, lighting, logo, and all other content. Do not crop, extend, redesign, or add elements. Return a clean background in the original layout.
```

## 不可承诺的情况

内置生成式编辑本身不能保证源图非文字像素不变，也未必返回原尺寸。只有完成原图合成与像素校验后，才可宣称修改区域外的像素保持不变。四边插值和羽化可以消除部分接缝，但无法还原被原字遮住的任意纹理或商品细节；有纹理、阴影或商品遮挡时不能硬用。排版脚本能避免生成模型扭曲字形，但不能自动匹配任意品牌字体。严格无瑕疵的要求以逐图验收为准，不能对尚未检查的整套图片作保证。

